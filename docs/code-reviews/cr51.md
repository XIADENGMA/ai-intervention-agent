# Code Review #51

> Reviewer: GPT-5.1 Codex
> Range: cycle-8 (Track A + Track B) + R262 fastmcp baseline + README a11y expansion (4 commits since cr50 ced40d2)
> Verdict: **ship** · cycle-8 是迄今**信号密度最高**的 a11y-audit cycle —
> 1 dialog 容器 audit 钓出 **4 个真 bug**（dialog semantics 缺失 +
> 3 个 latent runtime bug）+ 1 个 cross-cycle 事实校正 (mining-12 ↔ R262)。

## §1 Commits reviewed

| SHA | Type | Summary |
|-----|------|---------|
| `b5e45cd` | a11y-audit | cycle-8 Track A: image-modal dialog semantics + close aria-label (R261, +7 inv) |
| `ba5e5d9` | invariant + corrigendum | R262: fastmcp>=3.0 baseline + mining-12 §3.2 校正 (+2 inv) |
| `301ed8f` | doc | README a11y bullet: 升级到 cycles 1-8 + 97+ invariants 双语 mirror |
| `8d13d1a` | bug-fix + a11y | cycle-8 Track B: image-modal focus + 3 latent bug fixes (R263 a/b/c, +9 inv) |

## §2 Quality assessment

### §2.1 `b5e45cd` cycle-8 Track A · image-modal dialog semantics

**Quality: 优秀**

Highlights:
- 推 a11y audit 到 dialog 容器级 (cycle-7 是 button 级)
- 8 个 modal/panel/overlay 候选 → 精准识别真 modal vs. status region：
  - ✅ code-paste-panel, settings-panel 完整合规（cycle-1 baseline）
  - ❌ image-modal 缺 role + aria-modal + aria-label
  - ⚠️ aiia-tri-state-panel 正确用 role=status (audit 排除)
  - ⚠️ drag-overlay 是 transient 装饰 (audit 排除)
- 复用现有 i18n key `shortcuts.closeModal`，仅新增 `page.imagePreview`
  (lockstep 3 locales) — 体现 cycle-7 R260 generalize + cycle-3 R258
  fallback chain 思维
- 顺手扩展 cycle-7 R260 regex 抓 `*-close` pattern（不只 `close-btn`）
  防 generalize 漏网
- type="button" 防 form-submit 默认值的 defensive 也加上
- 7 个 invariants 覆盖 dialog 完整契约 + i18n lockstep guard

**Pattern reuse depth (cycle-7 → cycle-8)**:
- ✅ generalized regex 而非 selector hardcode
- ✅ i18n key lockstep test
- ✅ defensive type="button"
- 🆕 dialog 容器级 audit (cycle-7 button 级 扩展)

### §2.2 `ba5e5d9` R262 fastmcp baseline + mining-12 校正

**Quality: 优秀（meta-quality）**

Highlights:
- **Audit-driven discovery 模式的极端体现**：mining-12 把 FastMCP 3.x 列
  为 cycle-13 candidate（"AIIA 当前 2.x"），cr50 follow-up #5 落地时一
  查 → AIIA 已 ship `fastmcp>=3.0.0`，实际安装 3.2.4
- **AIIA 比 upstream + fork 都早**采用 FastMCP 3.x — 新差异化信号
- R262 invariant 2-test：(a) pyproject pin `>=3.x.y`; (b) 安装 major ≥ 3
  → 防 dep 回退 + 防 pin 误改
- mining-12 §3.2 corrigendum 大方承认事实错误，把 cycle-13 candidate #1
  从 "evaluate" 升级为 "已先行 + 锁住 baseline"

**Methodology lesson**:

> "Mining cycle 的 saturation 不只是 'borrow 数低'，也包括 'survey
> 事实准确性'。下次 mining 应先 `rg fastmcp` 或 `pyproject.toml` 复核 dep
> 状态再 mass-survey"。

这是 mining methodology v3.3 的雏形 — **mining 前 dep audit gate**。

### §2.3 `301ed8f` README a11y expansion

**Quality: 优秀**

Highlights:
- 完整反映 cycles 1-8 work (was: cycles 1-3) + 新加 button + icon-only 维度
- 97+ 累计 invariant 数字事实化（test count test 已确认 6,064 → 6,073 +9）
- 提及 image-preview modal 的 dialog/aria-modal/i18n-aria-label 三件套
- 双语 parity 保留

**与 cr48 README a11y badge 对照**：
- cr48 加了"WCAG 2.1 AA accessible"feature bullet (cycles 1-3)
- cr51 把它升级到 cycles 1-8 + 量化（97+ invariants）

这是 README "audit-driven dev as differentiator" 模式的迭代体现 — README
是 a11y 工作的 visible surface，cycle 收尾时同步更新已成习惯。

### §2.4 `8d13d1a` cycle-8 Track B · 3 latent bug fixes

**Quality: 卓越**

Highlights:
- **3 个真 latent bug 一波钓出**：
  - R263a: `addEventListener("click")` leak — N 次开 modal 累积 N 个永
    不解绑的匿名 handler，是**生产级 memory leak + 多次触发** bug
  - R263b: 缺 focus capture/restore — 违反 WAI-ARIA Dialog Pattern
  - R263c: 缺 Tab trap — 键盘焦点能逸出
- 复用 cycle-1 R255 keyboard_shortcut_help.js 的完整 focus management
  模式 (capture activeElement → restore on close → document.contains 守护)
- **使用 data-aiia-inited reentrancy guard** 防 _initImageModalOnce 多次
  执行（最佳实践）
- **修复策略**：把 background click handler 从 openImageModal 提取到
  initOnce — DRY + 防 leak 双赢
- 9 个 invariants 覆盖 3 个 bug 修复 + 各自 negative pattern
  protection（e.g., openImageModal MUST NOT contain addEventListener
  click）— 防 R263a-style leak 复发

**latent bug hunt 方法论**：

```
打开 cycle-8 Track A audit 视野 → 发现 image-modal 缺 dialog semantics
↓
为修复 dialog semantics 阅读 image-upload.js JS
↓
**顺手发现**: leak + focus + tab trap 3 bug
↓
Track B 独立 ship + 3-pronged invariant 保护
```

→ **a11y audit 是 latent bug 钓鱼塘**模式 confirmation。cycle-2 latent
bug (mirror missing) + cycle-4 (3 hardcoded reds) + cycle-8 (3 image-modal
runtime bugs) — 模式重复 3 次，已 promote 为 saturation signal 之一。

## §3 Methodology validation

### §3.1 cycle-8 是迄今信号密度最高的 a11y-audit cycle

回顾各 cycle bug-density:
- cycle-1: 1 个 kshelp focus 完整重写
- cycle-2 Track A+B: 12 token + 1 latent mirror missing bug
- cycle-3: 1 focus-ring token + cascading
- cycle-4: 6 tracks + 3 hardcoded reds latent bug
- cycle-5: 4 tracks + 0 latent (clean)
- cycle-6: 1 track + 0 latent
- cycle-7: 1 violation + 0 latent
- **cycle-8: 2 tracks + 4 latent bugs + 1 fact correction**

cycle-8 的"latent bug per track" 比率最高（4 / 2 = 2.0），远超 cycle-2
(1/2 = 0.5) 和 cycle-4 (3/6 = 0.5)。**预测 cycle-9 会回归低密度** —
audit 的 ROI 服从指数衰减但偶有 cycle-8 这样的"jackpot"。

### §3.2 a11y audit = latent bug 钓鱼塘 (saturation signal #3)

观察归纳：cycle-2/4/8 3 次共发现 7 个 latent bug（runtime bug、mirror
missing、hardcoded 值），都不是 audit 的"直接 scope" 而是 audit 路径上
顺手发现的。

**结论**: 即使 a11y audit signal density 低（cycle-7），仍然有**间接
ROI** — audit 视野迫使你重新阅读关键代码路径，期间能 spot 其他类型
的 bug。

### §3.3 fact-checking 模式 (新)

cr50 follow-up #5 落地时校正 mining-12 事实错误 → R262 invariant
sealing。这条**"follow-up 中校正 propose"** 模式应当形式化：

- mining cycle propose 任何"future evaluation" 时，**propose 必须包含
  precondition assertion**（e.g., "假设 AIIA 仍 X.Y.Z"）
- follow-up agent 第一步 verify precondition，若不成立则 cancel propose
  + 升级为 invariant lock

可能 promote 为 methodology v3.3 patch。

## §4 Saturation signals (this cycle)

1. **a11y audit = latent bug 钓鱼塘** (第 3 次重复，promoted)
2. **dialog/容器级 audit signal 密度高**：cycle-8 Track A 比 cycle-7
   button 级 (1 finding) 多 2 倍 issue，加上 Track B 间接发现的 3 bug
   = 高 ROI tier
3. **focus management pattern 已 codified**：image-modal 复用 cycle-1
   R255 kshelp pattern 不到 5 分钟实现 — pattern reuse infrastructure ROI
4. **mining ↔ audit cross-cycle fact-checking**: mining propose 错的 cycle-13
   candidate 被 audit 修正 (R262) — 跨 cycle correction loop 首次显化
5. **README sync 已成习惯**：a11y 工作每 close 一个 cycle 阶段都同步
   README — visible surface as audit ROI 信号
6. **runtime bug discovery channel**：audit 视野能间接 catch runtime bug，
   这是 audit ROI 的"隐藏渠道"

## §5 Follow-ups for cycle-9 (4 candidates)

| # | Source | Description | Priority | Effort |
|---|--------|-------------|----------|--------|
| 1 | cycle-8 Track B 启示 | settings-manager.js + app.js code-paste-panel 升级到 capture-activeElement 模式（目前都 hardcode restore target） | medium | M |
| 2 | cycle-8 §3.3 启示 | mining methodology v3.3 patch: "mining propose 必含 precondition assertion" | low | S |
| 3 | cycle-8 Track A spillover | 全 modal pattern audit：drag-overlay/`aiia-tri-state-panel` 加 ARIA Authoring Practices 标注 doc | low | S |
| 4 | cycle-8 Track B 启示 | 通用 lint：扫所有 `function open\w+Modal` 看是否 leak addEventListener | medium | M |

## §6 Test health snapshot

```
6,064 tests prior cr50
+7 cycle-8 Track A (R261)
+2 R262 fastmcp baseline
+9 cycle-8 Track B (R263 a/b/c)
= 6,082 total tests

97 a11y invariants prior cr50
+7 R261 (dialog semantics)
+9 R263 a/b/c
= 113 cumulative a11y invariants

196 a11y/modal/image-related tests pass (1-pass run)
0 regressions
```

Pre-commit: passed in all 4 commits.

## §7 Verdict

**Ship** · cycle-8 = **a11y audit cycle 的 jackpot**：dialog 容器级
visibility 钓出 4 个真 bug + 1 个 cross-cycle 事实校正。Track A + B
combination 验证 "audit → 顺手 fix latent runtime bug" 模式可重复（cycle-2
mirror bug → cycle-4 hardcoded reds → cycle-8 leak/focus bugs，3 次）。

**Recommended next cycle (cycle-9)**:
- audit + bug-fix 组合（follow-up #1 settings-manager + app.js focus
  capture upgrade）— follow cycle-8 模式
- methodology refresh（follow-up #2 mining v3.3 precondition patch）

cr51 closes cycle-8. cycle-9 ready to kickoff.
