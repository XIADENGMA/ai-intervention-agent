# Code Review #50

> Reviewer: GPT-5.1 Codex
> Range: cycle-6 (Track A + B) + cycle-7 Track A + CONTRIBUTING token table (4 commits since cr49 30c846f)
> Scope rule: ~5 commits since last review (cr49 closed cycle-4+5, 4 commits is acceptable for cycle-6+7 atomic combo)
> Verdict: **ship** · audit-mining 交替模式 + invariant 沉淀模式都 validate 成功

## §1 Commits reviewed

| SHA | Type | Summary |
|-----|------|---------|
| `abd6adc` | a11y-audit | cycle-6 Track A: `.btn-secondary` WCAG 1.4.3 fix (R259i, +5 inv) |
| `38a8daa` | mining-doc | mining-cycle-12: mcp-fb v2.6.0 + -pro v3.0.4 survey (doc-only, 0 borrow, 4 lessons) |
| `5cfb9a8` | a11y-audit | cycle-7 Track A: icon-only button `aria-label` (R260, WCAG 4.1.2, +2 inv) |
| `cdac291` | doc | CONTRIBUTING §3.quinquies: Standard CSS a11y token table (cr49 §5 #3) |

## §2 Quality assessment

### §2.1 `abd6adc` cycle-6 Track A · `.btn-secondary` WCAG 1.4.3

**Quality: 优秀**

Highlights:
- Direct apply of cycle-5 Track D methodology pattern (P1 computation-first
  + P6 audit-but-keep-:active brand color) — pattern reuse confirmed
- New helper `_composite_alpha_white(alpha, bg_hex)` for "translucent color
  on dark base" Source-Over composite — extends invariant infra for any
  future translucent component audit
- Dark theme correctly **NOT modified** (12:1 AAA already) — audit ≠
  uniformity (cycle-5 P6) explicitly validated
- Light theme: `#cc785c` → `#a85234` (3.28 → 5.36:1) for default; hover
  `#b86a50` → `#8a4525` (4.03 → 7.10:1) — Book Cloth family preserved
- R65 `.btn-secondary:active` retained `#c56a4c` (R65 invariant intact)

Pattern reuse depth (cycle-5 → cycle-6):
- ✅ same `_extract_bg_from_rule` iteration-first design
- ✅ same `assert is not None` for ty-check
- ✅ same 2-test-per-theme structure
- ✅ same `TestActiveStillUsesBrandOrange*` R65 guardian
- 🆕 dark theme composite helper (cycle-5 was opaque)

**Minor improvement opportunities:**

- `_composite_alpha_white` assumes alpha is the foreground's alpha; for
  multi-layer composites (e.g., `rgba` over `rgba`) the helper needs
  extension. Note in test docstring for future audits.
- Hex value `#1a1a1a` for `DARK_BG_PRIMARY` is hardcoded — could read
  from CSS like cycle-2 does. Acceptable since `#1a1a1a` is in cycle-2's
  documented baseline; if dark `--bg-primary` changes, cycle-2 test
  catches first and this test naturally follows. **No action**.

### §2.2 `38a8daa` mining-cycle-12 · 4th saturation signal

**Quality: 优秀（doc-only）**

Highlights:
- **0 ship borrow + 7 convergent + 1 not-applicable + 2 defer** —
  saturation signal across 10 features mapped to AIIA equivalents
- Discovers fork `mcp-feedback-enhanced-pro` (LeonBuild, 1 year of v3.0.x
  releases) used as **early-warning radar** for upstream trends (5-10
  cycle lead time pattern)
- §4.3 differentiation table: AIIA's edge is **运维深度 + 测试纪律 +
  a11y** (Test discipline 6,057 vs <100, WCAG 91 invariants vs 无), **not
  feature count**
- 3 cycle-13 candidates extracted: FastMCP 3 evaluation, session retention
  audit, gemini-cli / claude-code cross-product mining
- 4 lessons captured, especially L4: "mining cycle output 多元化 — doc +
  signal + future candidates 同样是有形输出" — formalizes mining-11's
  README-backfill pattern as cycle-level standard

**Methodology fit:**

mining-12 is the **third doc-only mining cycle** (mining-3 = ship; mining-5
= ship; mining-9 = ship; mining-10 = ship; **mining-11 = doc**;
**mining-12 = doc**). Two consecutive doc-only mining cycles confirm
saturation. Recommendation: pivot mining cadence:
- Audit cycle every 1-2 weeks (high signal)
- Mining cycle every 4-6 weeks (low signal, conserve research time)

Not in PR but worth noting: `feature-mining-cycle-12.md` does not appear
in any test file's invariant catalog, which is intentional — mining docs
are notes-only and not part of the regression baseline.

### §2.3 `5cfb9a8` cycle-7 Track A · `aria-label` (WCAG 4.1.2)

**Quality: 优秀**

Highlights:
- New a11y dimension opened (cycles 1-5 = focus + 1.4.3/1.4.11, cycle-7 =
  4.1.2 Name/Role/Value) — explicit cycle-kind shift signaled in doc
- Single button fix (`#open-config-file-btn`) but **generalized invariant**
  scans all `btn-icon-only|close-btn|tab-close|btn-circle` classes —
  prevents single-instance future regression
- Re-uses existing i18n key `settings.openConfigInIde` (3 locales) — no
  new key, no orphan-key sync risk
- 2nd invariant targets the specific button's `data-i18n-aria-label`
  presence (so future "removed by accident" is caught)

**Doc quality:**

`docs/a11y-audit-cycle-7.md` §3 saturation observation is sharp:
"signal density演进 cycle-1/2/3 高 → cycle-5 中 → cycle-7 低" with
prescription "Track 数量减少但 invariant 必须 generalize"。这是 cycle-3
generated saturation signal的延伸：cycle-7 是 a11y-audit cycle 第一次进
低密度区，doc 记录了应对策略。

**Minor improvement opportunities:**

- `ICON_BUTTON_CLASS_RE` is regex over `class="..."`, so does not handle
  CSS-in-JS dynamic class names. AIIA does not use CSS-in-JS so this is
  fine, but worth a note in the test docstring.
- 4 cycle-8 candidates documented but no immediate ship — consistent with
  mining-12 L4 ("doc + signal 已是有形输出")。

### §2.4 `cdac291` CONTRIBUTING §3.quinquies · CSS a11y token table

**Quality: 优秀**

Highlights:
- Codifies 5 cross-cycle CSS tokens into a single table with WCAG
  constraint per token + per theme — **discoverable for new contributors**
- Bilingual mirror (en + zh-CN) maintained
- Publishes the **canonical `:focus-visible` template** with mandatory
  triple-fallback chain `var(--focus-ring-color, var(--primary-500,
  currentColor))` — directly addresses the cycle-3-style regression
  pattern
- Footnotes `prefers-contrast: more` global adapter (cycle-1 R256) so new
  contributors know they inherit for free without manual `@media` block
- 与现有 §3.ter (recurring design constraint) + §3.quater (i18n wrapper
  checklist) 形成连贯的"recurring patterns" 章节群

**Methodology fit:**

This is the doc-rollup type of follow-up that classically lags behind
implementation cycles. cr49 §5 follow-up #3 listed this and now it's
shipped within 4 commits — fast follow-up cadence is ideal.

**Strong signal**: with §3.quinquies in place, future a11y audit cycles
can reference the table in test failure messages, e.g.:

```python
self.assertGreaterEqual(
    contrast, 4.5,
    f"... 见 CONTRIBUTING.md §3.quinquies token table"
)
```

This converts CONTRIBUTING from "passive reference" to "active error
hint", improving DX for any future contributor running tests.

## §3 Methodology validation

### §3.1 cr49 verdict 落地

cr49 recommended "audit ↔ mining 交替" — cycle-6 perfectly executed this:
- Track A = polish (audit, .btn-secondary)
- Track B = mining (mining-12 doc-only)

✓ Audit-mining 交替模式 validate 成功，避免单一维度疲劳。

### §3.2 Pattern reuse across cycles 5 → 6 → 7

| Pattern | cycle-5 (D) | cycle-6 (A) | cycle-7 (A) |
|---------|-------------|-------------|-------------|
| Computation-first invariant | ✓ btn-primary contrast | ✓ btn-secondary contrast | ✓ aria-label scan |
| Generalized vs hardcoded | partial (4 selectors) | partial (1 selector) | ✓ class-pattern regex |
| R65 :active 守护 | ✓ TestActive...Primary | ✓ TestActive...Secondary | N/A |
| Doc + lessons sweep | cycle-5.md | cycle-6 没有专门 doc | cycle-7.md + saturation |
| `assert is not None` ty-friendly | ✓ | ✓ | N/A (无 Optional 返回) |

**Pattern density 评分**：cycle-6 = 5/5 reuse · cycle-7 = 3/3 reuse +
generalize 升级。pattern infra 已成熟到 "无脑复用"，标志着 audit
cycle 的执行成本显著降低。

### §3.3 invariant counts

| Cycle | Invariants added | Cumulative a11y |
|-------|------------------|-----------------|
| cycle-1 (Track A+B) | 16 + 7 = 23 | 23 |
| cycle-2 (Track A+B) | 31 | 54 |
| cycle-3 (Track A) | 7 | 61 |
| cycle-4 (6 tracks) | 5+? | ~67 (with cycle-5 R259e merge) |
| cycle-5 (Track A-D) | 5 + 5 + 8 + 5 = 23 | 90 |
| **cycle-6 Track A** | **5** | **95** |
| **cycle-7 Track A** | **2** | **97** |
| **Total a11y invariants** | **97** | |

## §4 Saturation signals (this cycle)

1. **Pattern infra 成熟度**: cycle-6/7 几乎"无脑复用" cycle-5 的工具
   (`_extract_bg_from_rule`, computation-first, R65 守护)。新 audit cycle
   的执行成本显著降低 = **infrastructure ROI 正反馈**
2. **a11y-audit signal density 走低**: cycle-7 仅发现 1 个 violation。
   future a11y cycles 应当：
   - 拆 Track 为更广的 sweep (e.g., 7 Track A 已 generalize)
   - 期待 cycle-8/9 进入 "0-violation confirm" 模式 (类似 mining-11/12)
3. **doc-only cycle 成为正式形态**: mining-11 + mining-12 + cycle-7
   都把 doc/lessons 当作首要 ship 物 — "ship doc" 不再需要解释
4. **CONTRIBUTING 章节群定型**: §3.bis + §3.ter + §3.quater + §3.quinquies
   形成"recurring patterns" 章节群，每个 cycle 都能贡献 1-2 个章节
5. **fork radar 模式**: mining-12 把"观察 fork 比 upstream 激进"的现象
   formalize 为 5-10 cycle lead 时间窗口的 early-warning 工具 — 未来
   依赖管理决策可以参考此模式

## §5 Follow-ups (5 candidates for cycle-8)

| # | Source | Description | Priority | Effort |
|---|--------|-------------|----------|--------|
| 1 | cycle-7 Track B carry | modal/dialog role + aria-modal 一致性 audit | medium | M |
| 2 | cycle-7 Track C carry | form input aria-describedby for errors | medium | S |
| 3 | cr49 §5 #5 carry | README "audit-driven dev" badge / methodology mention | low | S |
| 4 | cycle-7 backlog | `<a>` tag 缺 link text audit (R260 同源 generalize) | low | S |
| 5 | mining-12 §5 #1 | FastMCP 3.x compatibility 评估调研 | medium | M |

## §6 Test health snapshot

```
6,057 tests prior cr49
+5 cycle-6 Track A
+2 cycle-7 Track A
= 6,064 total tests

155 a11y + brand + light_mode tests pass (1-pass run)
0 regressions detected
```

Pre-commit hooks: all pass (ruff-format auto-fix needed for SIM102 lint,
proves the hook is doing its job).

## §7 Verdict

**Ship** · cycle-6+7 是 cr49 "audit ↔ mining 交替" verdict 的标准范例
实施。pattern infra 成熟度反正反馈成立 — audit cycle 的执行成本进入下
降通道，doc-only cycle 已是正式形态。

**Recommended next cycle (cycle-8)**: 继续 audit-mining 混合：
- audit Track: cycle-7 Track B (modal aria-modal 一致性) 或 Track C
  (form aria-describedby)
- mining Track: 从 mining-12 §5 candidates 选一个 (推荐 FastMCP 3.x
  调研，dep EOL 风险最高)

cr50 closes cycles 6 + 7. cycle-8 kickoff ready.
