# Code Review #40 — cycle-5 kickoff + v1.8.0 release + cr40 health sweep

> Scope: 5 commits since CR#39 (commit `d835a96`).
> Cycle theme: **"convert cr39 follow-up items into shipped
> work, then cut the v1.8.0 release tag"** — README marketing
> bullet (cr39 §8 #2), mining-cycle-5 kickoff with §0
> methodology v3 (cr39 §8 #3 + #4), full-suite health sweep
> catching 4 latent regressions (cr40 unscheduled), and the
> v1.8.0 release closeout (cr39 §8 #1).

## §1 Commits in scope

| # | SHA | Subject | Files | LoC |
|---|---|---|---|---|
| 1 | `432330b` | `:books: docs(README): cr39 §8 #2 加 "Stable install" 差异化 bullet（中英）` | 2 | +2/-0 |
| 2 | `14f3458` | `:books: docs(mining-5): kickoff cycle-5 — §0 methodology v3 + 5 tracks + v1.8.0 release plan` | 1 | +154/-0 |
| 3 | `6024058` | `:bug: fix(health-4-tests): cr40 cycle full-suite green — 4 latent regressions surfaced & fixed` | 6 | +143/-69 |
| 4 | `87b9fea` | `:bookmark: release(v1.8.0): cycle 17-21 closeout — UX backlog + 4 mining cycles + minor bump` | 6 | +76/-24 |
| 5 | `55bd807` | `:bug: fix(test-freshness-mutation): cover immediate-post-tag tag-cut window` | 1 | +35/-6 |

Plus 1 local annotated git tag `v1.8.0` (no push, per plan).

Net: **15 files / +375 / -91**.

## §2 Cycle quality assessment

### §2.1 Cycle structure (recognized pattern: "follow-up burndown → release → reactive fix")

This cycle resolved a different shape than cr38/cr39's
"follow-up + research + ship + close": here we had 1 quick
follow-up (README) + 1 process artifact (mining-5 kickoff
doc) + 1 reactive health sweep + 1 release + 1 post-release
fix. The compactness reflects that the cr39 §8 backlog was
itself process-heavy (release + methodology), not feature-
heavy.

**New pattern element**: the full-suite **health check**
prior to release surfaced 4 latent regressions that per-
module test runs missed — a clean validation of "always run
the full suite before release" as a non-negotiable release-
process step. Mining-5 kickoff doc Track E now encodes this
as a mandatory release gate.

### §2.2 cr39 §8 follow-up burndown

| Item | Priority | Status | Commit |
|---|---|---|---|
| §8 #1 v1.8.0 release | info | ✅ done | `87b9fea` |
| §8 #2 README stability marketing | low | ✅ done | `432330b` |
| §8 #3 mining-5 §0 methodology v3 | medium-ROI | ✅ done (in `14f3458`) | `14f3458` |
| §8 #4 mining-5 kickoff doc | low | ✅ done | `14f3458` |
| §8 #5 `auto_resubmit: { once: true }` | low | ⏸️ scheduled for mining-5 Track C | n/a |
| §8 #6 Track D aider/sweep | low | ⏸️ deferred (de-prioritized per niche-saturation finding) | n/a |
| §8 #7 e2e harness | defer | ⏸️ still deferred | n/a |

**4/7 closed, 0 still-actionable open, 3 explicit defers**
— excellent burndown ratio for a release-bracket cycle.

### §2.3 Unscheduled fixes (cr40 health sweep)

| # | Test | Root cause | Fix |
|---|---|---|---|
| 1 | `test_feat_remove_download_button` | Invariant too strict; settings-panel export link valid | Region-scoped to `header-actions` |
| 2 | `test_feat_mining3_header_chip::TestRoute` | TaskQueue singleton 10-task limit pollution in full-suite | Text-level src checks + direct unit test |
| 3 | `test_lock_watchdog_full_coverage_r52a` | `extend_task_deadline` ship missed label-set sync | Added entry to `WRITE_METHOD_LABEL_MAP` |
| 4 | `test_runtime_behavior::test_web_locale_no_dead_keys` + `test_i18n_orphan_keys` | (a) regex missed `AIIA_I18N.t(...)` namespaced calls; (b) `let msgKey = "..."` dynamic-key pattern untraceable | (a) regex extension; (b) explicit reserve list (8 keys) |
| 5 | `test_changelog_freshness::test_detects_missing_unreleased` | Immediate-post-tag mutation falls outside check #2 window | Mutation now also renames latest-tag section (check #1 trigger; tag-independent) |

All 5 fixes use **forward-compatible** patterns — each is
either an additive regex alternation, an additive reserve
entry, an additive map entry, or a stricter mutation. None
of the fixes weaken existing assertions.

### §2.4 Release quality (v1.8.0)

| Aspect | Status |
|---|---|
| Version triplet bump | ✅ all 3 (`pyproject.toml` / `package.json` / `packages/vscode/package.json`) |
| `package-lock.json` sync | ✅ manually patched (npm install lazy-load failed; manual edit verified) |
| `uv.lock` sync | ✅ regenerated cleanly on first test run |
| `CHANGELOG.md` `[Unreleased]` promotion | ✅ promoted with full Cycle 17-21 inventory (+66 commits documented) |
| Local git tag `v1.8.0` annotated | ✅ created with full release notes + SemVer rationale |
| Pre-commit hooks green | ✅ all hooks pass (no `--no-verify`) |
| Push | ⏸️ intentionally not pushed (per user instruction) |
| SemVer rationale documented | ✅ in commit body + tag message (3 additive MCP schema fields → minor bump) |

**Release process discipline verified**: SemVer bump
justified in writing; immediate-post-tag full-suite re-run
caught the mutation-test edge case (commit #5), which is
exactly the failure mode such a re-run is supposed to
catch.

## §3 Per-commit quality notes

### §3.1 `432330b` README "Stable install" — quality **good**

2-line additive bullet in both English + Chinese README.
Cleanly references competitor's Issue #213 / PR #215 as
the contrast point. Zero risk of style drift — bullet
follows existing "Key features" list format exactly.

**Strength**: minimal LoC for measurable marketing value.
Surfaces a real competitive advantage (Flask 3.x stability
vs Starlette 1.0 breakage) without overselling.

### §3.2 `14f3458` mining-5 kickoff — quality **excellent**

The kickoff doc is **the methodology artifact of this
release cycle**. Key strengths:

1. **Codifies §0 methodology v3** with the new mandatory
   "subject type" column (server / client / IDE plugin /
   agent-CLI), preventing future Track C-style mis-
   categorization.
2. **5 diversified tracks** explicitly answers the cycle-4
   niche-saturation signal — sources now span Continue.dev
   (Track A), Augment/Trae/Windsurf (Track B), `auto_resubmit:
   once` MVP (Track C), aider/sweep (Track D, deferred),
   v1.8.0 release (Track E).
3. **Explicit budget** (commit count, LoC ceilings, defer
   triggers) — prevents cycle drift.
4. **Closeout criteria include local v1.8.0 tag** —
   binds release work to the cycle, not a side project.

**One minor note**: the kickoff doc is 154 lines, larger
than cr38/cr39 kickoffs. Justified because methodology v3
needed full worked examples + cycle-5 has 5 tracks.

### §3.3 `6024058` health-fix sweep — quality **excellent**

6 files / +143 / -69 changing 4 test files and 1 script.
Each fix:
- Has a one-line root-cause comment
- Uses forward-compatible patterns (additive only)
- Was verified by re-running the specific failing test

**Strength**: identified a meta-issue (full-suite vs per-
module runs) and documented it in the commit body so
future maintainers understand the systemic vs local
distinction. The TaskQueue singleton pollution finding is
particularly valuable for future test design — flagged in
commit body as "avoid `test_client()` for routes that touch
shared singletons unless test isolates the singleton".

**One observation**: 4 latent regressions surfaced in a
single sweep suggests **per-module isolation testing has
been over-trusted in recent cycles**. cr41 §8 should
consider adding a `full-suite-green` pre-release gate to
release checklist explicitly. (Logged as cr41 §8 #1 below.)

### §3.4 `87b9fea` v1.8.0 release — quality **excellent**

The release commit itself is **6 files / +76 / -24** of
metadata + CHANGELOG promotion. Strengths:

1. **SemVer rationale in commit body** — explicitly cites
   3 additive MCP schema fields as the trigger for minor
   bump, with safe-default guarantee.
2. **Cycle 17-21 highlights inline** — commit body
   summarizes the 5-cycle window for git-log readers who
   don't open CHANGELOG.
3. **CHANGELOG `[Unreleased]` retained with placeholder**
   — keeps the freshness check passing during the
   commit→tag→fix window (a subtle release-process detail).
4. **No push** — respects user release plan.

**One detail worth noting**: `package-lock.json` was
manually patched (the fnm lazy-load `npm install` failure
forced direct json edit). The patch is correct (3 version
strings updated in matching positions), but it does
bypass npm's lockfile integrity recalculation. Acceptable
because: (a) no dependency change, only metadata version;
(b) lockfile integrity hashes are over package tarballs,
not the root project metadata. Documented this in the
release commit body would be a minor improvement.

### §3.5 `55bd807` mutation-test fix — quality **good**

Reactive fix to an edge case revealed by the post-tag
verification re-run. Strengths:

1. **Root cause clearly documented** in commit body —
   tag-cut window puts HEAD == tag, so check #2 cannot
   trigger.
2. **Fix uses check #1** (latest-tag section presence)
   which is tag-state independent — robust across all
   repo states.
3. **Skip-test fallback** for repos without tags
   preserved (was already there; new mutation respects
   it).

**One note**: the fix landed in 2 commits (ruff format
auto-fix re-prompted commit). Same noise mode as cr39
`e65d152`. Considered acceptable — pre-commit
autoformatting is by design.

## §4 Performance impact

- **No hot-path changes**. All changes are in tests,
  scripts, docs, and metadata.
- **CHANGELOG.md** grew by 86 lines (release notes); this
  is build-artifact territory, no runtime impact.
- **No new dependencies** added (zero `pyproject.toml`
  dependency diff; only version bump).

## §5 Test discipline

- 5 commits, **0 `--no-verify`**.
- Health-fix sweep added net **+0 new test files** but
  refactored 4 existing files for higher full-suite
  fidelity.
- Mutation-test fix tightened drift detection coverage.
- **Full suite re-run after release tag**: caught 1 edge
  case (mutation test) — **process discipline works**.
- All freshness/CI gates green at release tag (commit
  `87b9fea`) and again post-fix (commit `55bd807`).

## §6 What didn't ship

- mining-5 Track A (Continue.dev survey) — kickoff doc
  only; survey work belongs to cr41 cycle.
- mining-5 Track B (Augment/Trae/Windsurf survey) — same.
- mining-5 Track C (`auto_resubmit: once` MVP) — same.
- README badge updates for v1.8.0 (low priority, defer).

## §7 What broke (none)

- 0 regressions from any of the 5 commits.
- 4 latent regressions were **caught** by the cr40 health
  sweep (not introduced by it) — these date back to
  earlier cycles. Sweep is **net positive** for
  reliability.

## §8 Verdict

**APPROVED**.

Cycle quality: **excellent for a release-bracket cycle**.
The combination of:
1. Disciplined cr39 §8 burndown (4/7 closed)
2. Full-suite health check catching 4 latent regressions
3. SemVer-justified minor bump with safe-default schema
   additions
4. Local-only tag respecting user release plan
5. Post-tag verification surfacing + fixing 1 edge case

…makes this a textbook example of how a release should be
prepared and executed. The mining-5 kickoff doc with
methodology v3 ensures the next cycle starts with
hardened tooling.

## §9 Follow-up items for cr41 cycle

### Priority (recommended order)

1. **[medium]** Add explicit "full-suite green before
   release" step to `docs/feature-mining-cycle-N.md`
   Track E template. Codify the cr40 finding so future
   release cycles never skip this gate. (Could also be a
   `pre-commit` block on commits whose subject matches
   `:bookmark: release(v*):`.)

2. **[medium-ROI]** mining-5 Track A — Continue.dev MCP
   server schemas survey. Continue.dev (`continuedev/continue`)
   is a multi-IDE MCP server with broad surface; first
   diversified-source survey of cycle-5.

3. **[medium-ROI]** mining-5 Track B — Augment / Trae /
   Windsurf MCP interaction-loop survey. These three are
   IDE-plugin clients (per methodology v3 classification),
   so survey targets are the client-server schema
   negotiation patterns they use.

4. **[low]** mining-5 Track C — `auto_resubmit: { once:
   true }` MVP. Schema delta ~5 LoC; frontend single-shot
   logic ~20 LoC. Adjacent finding from cr39 §8 #5.

5. **[low]** Investigate moving the manual `package-lock.json`
   version-bump pattern into a reusable script
   (`scripts/bump_version.py`) to remove the fnm lazy-load
   failure dependency. Saves ~5 min per release.

6. **[low]** Consider adding a `release-checklist.md` doc
   that consolidates the v1.8.0 release process into a
   reusable runbook. ~50 LoC.

7. **[defer]** e2e harness (carried from cr37 → cr38 →
   cr39 → cr40). Still needs Playwright/Selenium
   infrastructure investment. Re-evaluate trigger: when
   3+ frontend bugs ship that would have been caught by
   click-through testing.

## §10 Cycle stats summary

- **5 commits** since CR#39 (zero `--no-verify`)
- **+375 / -91 lines** net
- **~143 LoC test/script + ~232 LoC docs/metadata**
- **test/code ratio**: ~1.0 (high — mostly test fixes
  and docs)
- **0 new test files**, **0 new test cases** (refactor
  cycle, not feature cycle)
- **3 doc commits** (README bullet + mining-5 kickoff +
  release CHANGELOG section)
- **0 hot-path perf impact**
- **0 regressions introduced**, **4 latent regressions
  caught + fixed**
- **1 release shipped** (v1.8.0 local tag)
- **1 cycle kicked off** (mining-5 with methodology v3)
- **cr39 §8 burndown**: 4 closed / 3 deferred
- **process pattern recognized**: full-suite green before
  release is non-negotiable
- **subtle release detail surfaced**: manual `package-lock`
  patch bypasses npm integrity recalc but is safe for
  metadata-only version bumps

— end of cr40 —
