# cr47 · 5-commit cycle review — mining-11 closeout + a11y-audit-cycle-1 (first-ever) + 2 cr46 follow-ups

> Status: **review complete**
> Window: 5 commits since cr46 (mining-10 closeout)
> Most notable shift: **first invocation of `a11y-audit`
> cycle kind**, validating cycle-10/11 infrastructure
> investment (`*-cycle-*.md` glob + filename convention).

---

## §1 Commit table

| # | SHA | Title | Files | LoC delta |
|---|---|---|---|---|
| 1 | `f899f46` | docs(mining-11): cycle-11 kickoff + Track A — README backfill 5 cycle-1-3 features (cr46 §7 #7) | 4 | +100 |
| 2 | `badd634` | test(template-naming): mining-11 Track B + cycle-11 closeout — filename convention + Russell paradox marker fix (R254) | 4 | +187 / -26 |
| 3 | `2da39d2` | docs(readme): cr46 §7 #1 — split "Stable install" bullet for visual hierarchy | 3 | +12 / -2 |
| 4 | `dc76099` | feat(kshelp-a11y): a11y-audit-cycle-1 Track A — kshelp focus return + Tab trap + sibling inert (R255) | 8 | +585 |
| 5 | `dc48ee2` | feat(prefers-contrast): a11y-audit-cycle-1 Track B (R256) — prefers-contrast: more focus indicator upgrade + cycle-1 closeout | 8 | +227 / -8 |

Cycle nature: **2 follow-ups + 1 closeout + first-ever
a11y-audit cycle (with 2 ships)**. 5 commits, 0
regressions.

---

## §2 Per-commit review

### §2.1 `f899f46` README backfill — quality **excellent**

**Scope**: cycle-11 Track A. Adds 3 new "Key features"
bullets to README.md + README.zh-CN.md covering 5
previously-missing user-facing features (productivity
shortcuts, quick reply phrases, custom notification
sound).

**Quality**:
- ✅ Bilingual parity: both en + zh-CN updated together
  (continues post-R145 dual-locale invariant)
- ✅ Each new bullet references specific R-IDs in commit
  message (R130, R131d, R138, R139, R140, R144, etc.) —
  enables `git blame` archaeology
- ✅ Closes cr46 §7 #7 medium follow-up in 1 cycle
  (continues cycle-10 lesson "no follow-up drift")

**Concerns**: none.

### §2.2 `badd634` template-naming + Russell paradox fix — quality **excellent**

**Scope**: cycle-11 Track B. Three deliverables in one
commit:
1. Template §0.0 `<kind>-cycle-<N>.md` filename
   convention codified
2. `TestCycleDocFilenameConvention` test class added
   (regex-validated invariant)
3. Boilerplate-detection prose fingerprint refined from
   substring (cycle-10's first iteration) to full
   cross-line template-internal snippet

**Quality**:
- ✅ Russell's-paradox failure mode caught & fixed in
  the **same** commit that introduced the test (no
  separate "fix the broken test" commit)
- ✅ Regex pattern `^[a-z][a-z0-9]*(?:-[a-z0-9]+)*-
  cycle-\d+\.md$` correctly captures kebab-case kind +
  positive integer N + exact separator
- ✅ Template exclusion test ensures the template itself
  is correctly **not** subject to the naming check (its
  name `feature-mining-cycle-kickoff-template.md`
  intentionally doesn't follow `<kind>-cycle-<N>` per
  §0.0 since it's not a cycle doc, but a template)
- ✅ Hygiene check now generalizes to **all** cycle
  kinds — first immediate beneficiary is `a11y-audit-
  cycle-1.md` shipped 2 commits later (cycle-11 Track
  B's investment paid back same cr47 window)

**Concerns**:
- 📌 Pre-commit hook auto-fixed Ruff style (multi-line
  regex → single-line) → required re-staging. Standard
  workflow; documented in cycle-11 forward log.

### §2.3 `2da39d2` Stable install split — quality **good**

**Scope**: cr46 §7 #1 micro-fix. Splits the longest
single-line "Key features" bullet into header + nested
sub-bullet for the Starlette breaking-change comparison.

**Quality**:
- ✅ Both en + zh-CN updated symmetrically
- ✅ No tests reference the original prose verbatim
  (confirmed via `rg "Stable install" tests/`)
- ✅ Headline claim ("built on Flask 3.x with
  conservative dependency pins") now reads cleanly at
  first scan; differentiator detail preserved at deeper
  visual indent

**Concerns**: none.

### §2.4 `dc76099` kshelp focus management — quality **excellent**

**Scope**: a11y-audit-cycle-1 Track A. `keyboard_
shortcut_help.js` adopts the settings-panel a11y modal
pattern in full:
- `_previouslyFocusedElement` captured before
  `appendChild` (critical ordering)
- `_onTabInOverlay` keydown trap with `card` refocus
  (0-focusable variant)
- `_setContainerSiblingsInert(true|false)` via
  `_safelySetInert` (mirrors settings-manager.js exactly)

**Quality**:
- ✅ **Code reuse via mirroring** (not direct import) —
  IIFE module pattern doesn't naturally expose helpers
  to other modules; mirroring `_safelySetInert` keeps
  kshelp self-contained while ensuring behavioral
  parity. **Documented in code comments** that pattern
  matches settings-manager.js.
- ✅ **Symmetric add/remove** for both `inert` setting
  AND `keydown` listener — prevents leaks (cycle-10
  R252 ping backoff cleanup lesson applied).
- ✅ **`document.contains(prev)` staleness guard** —
  protects against `prev` being removed before
  `hideOverlay` (e.g., task tab closed while overlay
  open). This isn't a hypothetical — task-tab DOM is
  highly dynamic per R131c.
- ✅ **Double-tier `focus()` fallback** — first try
  `focus({preventScroll: true})`, then bare `focus()`,
  finally silent skip. Defends against older browsers +
  arbitrary opener elements (e.g., body) that may not
  be focusable but reaching `focus()` is harmless.
- ✅ **`_previouslyFocusedElement = null` reset** —
  prevents next `showOverlay` reading stale reference
  if hide was already called.
- ✅ 16 invariants cover all behavioral guarantees,
  including the **read-activeElement-before-append**
  ordering (use of `body.find()` index comparison).
- ✅ 0 regressions in R144 31-test suite.

**Concerns**:
- 📌 `_onTabInOverlay` registers on `document` capture
  phase. If user has another `document keydown true`
  listener registered AFTER kshelp (e.g., dev tool),
  both fire — both have to be Tab-respecting. Currently
  no other capture-phase Tab handler exists in code.
  **Defer**: not exploitable, just an architectural note.

### §2.5 `dc48ee2` prefers-contrast: more — quality **excellent**

**Scope**: a11y-audit-cycle-1 Track B bonus. Adds
`@media (prefers-contrast: more)` block inside
`@layer a11y` upgrading `:focus-visible` outline to
4px / 3px offset / `Highlight` color.

**Quality**:
- ✅ **`@layer a11y` placement** — ensures cascade
  priority over component CSS regardless of source
  order (CSS @layer spec). This is the **correct
  primitive** for a11y-overrides; settings-manager
  modal CSS doesn't need explicit layer because it's
  in a deeper @layer organization.
- ✅ **System `Highlight` color** instead of brand —
  rationale documented in code comment AND commit
  message. This is sophisticated a11y: respects user's
  chosen contrast scheme rather than imposing brand.
- ✅ **Only `:focus-visible`** — preserves no-outline
  behavior for mouse clicks (matches existing a11y
  layer philosophy). Documented as invariant in tests.
- ✅ 7 invariants cover block existence, layer
  placement, all 3 value upgrades, `:focus-visible`
  scope, and R-ID/cycle attribution.
- ✅ Test helper `_extract_media_block` uses
  brace-counting fallback when regex fails — mirrors
  cycle-9 lesson #1 (greedy regex problem from PWA
  offline tests).
- ✅ Closeout of a11y-audit-cycle-1 in same commit —
  no follow-up needed.

**Concerns**:
- 📌 `Highlight` keyword is **deprecated** in modern
  CSS color spec (replaced by `AccentColor` /
  `HighlightText`/forced-colors specific keywords).
  Browser support for `Highlight` is universal **today**
  but a future browser may warn. **Mitigation**: if
  cycle-N notes warnings, the fallback `outline-color:
  var(--primary-500)` from the non-media rule still
  applies (CSS cascade). **Acceptable risk**.

---

## §3 Methodology validation

### v3.2 compliance (all 5 commits)

- ✅ `f899f46`: `rg` evidence in cycle-11 doc for
  README backfill candidates (5 features checked against
  README before adding)
- ✅ `badd634`: cycle-11 Track B candidates validated
  via `rg` for existing cycle docs naming compliance
- ✅ `2da39d2`: `rg "Stable install" tests/` evidence
  in commit message ensuring no invariant breakage
- ✅ `dc76099`: cycle-1 doc §1 inventory built via
  `rg -l 'role="dialog"|aria-modal'`
- ✅ `dc48ee2`: cycle-1 backlog item check via `rg -c
  'prefers-contrast|prefers-reduced-motion'` (found 0/8
  → real gap)

### Track F template adoption

- ✅ Both cycle-11 and cycle-1 use the standard forward
  log table format
- ✅ Both have §0 methodology section + §4 closeout
  criteria + §5 lessons + §7 forward backlog

### §0.0 filename convention (cycle-11 Track B output)

- ✅ Both `feature-mining-cycle-11.md` and `a11y-audit-
  cycle-1.md` conform to `<kind>-cycle-<N>.md`
- ✅ Cycle-11 Track B regression test passes for both

**Meta-observation**: cycle-11 Track B's filename
convention validated within 2 commits by cycle-1's
naming. Infrastructure-to-use latency = 2 commits.

---

## §4 Saturation signals

| Signal | Evidence | Interpretation |
|---|---|---|
| 7× single-cycle execution | cycle-5..11 + cycle-1 all single-window | Steady state, was retired as notable in cr46 |
| **New cycle kind invoked** (`a11y-audit`) | cycle-1 opened/closed in cr47 window | **Infrastructure investment validated** within 1 cr cycle of being shipped |
| Russell's-paradox marker fix | cycle-11 Track B needed mid-cycle refinement of cycle-10 Track C's fix | **Recursive hygiene check pattern** — discussed in lesson #1; not a defect but a meta-pattern |
| Bonus tracks during audit cycles | cycle-1 Track B emerged organically | Continues cycle-10 R253 cross-tab theme sync pattern |
| Mining → audit cycle shift | cycle-9..11 audit/doc; cycle-1 audit | Project natural maturity — fewer external borrows available, more internal audit value |
| 16+7 = 23 new invariants in 1 cr cycle | tests added in 2 commits | Test discipline maintained; no "ship now, test later" |

---

## §5 Follow-ups

| # | Severity | Item | Defer to |
|---|---|---|---|
| 1 | low | `_onTabInOverlay` capture-phase registration interaction with hypothetical future `document keydown true` handlers — defensive doc note in source | cycle-2 |
| 2 | info | `Highlight` keyword forward-compat → modern `AccentColor` / `HighlightText` per WCAG forced-colors spec | when browser warning emerges |
| 3 | low | a11y-audit-cycle-2 candidate: keyboard-only nav full user-journey walkthrough | cycle-2 |
| 4 | low | a11y-audit-cycle-2 candidate: WCAG AA color contrast ratio audit on theme tokens | cycle-2 |
| 5 | info | VoiceOver/TalkBack manual testing (needs hardware) | indefinitely deferred per cycle-9 pattern unless device becomes available |
| 6 | info | Methodology meta-doc: should we promote `<kind>-cycle-<N>` to a doc taxonomy diagram? (4 kinds now: feature-mining/perf-audit/a11y-audit + template) | when 5th kind appears |
| 7 | info | cr45 #3 R-ID archaeology — still pending | low priority |
| 8 | info | cr45 #6 v1.9.0 release gate | next release |

---

## §6 Test health

cycle-11 + cycle-1 added 23 new tests across 4 modules:
- `test_feat_mining9_cycle_doc_no_boilerplate.py`: 10 →
  12 (+2 naming convention invariants)
- `test_feat_a11y_cycle1_kshelp_focus.py`: new file
  (16 tests)
- `test_feat_a11y_cycle1_prefers_contrast.py`: new
  file (7 tests)

All passed locally. No full-suite gate run (non-release
cycle window).

---

## §7 Conclusion

cr47 = **2 follow-up closes + cycle-11 closeout + first-
ever a11y-audit-cycle-1 (with 2 ships)**. The 5 commits
together demonstrate:

1. **Infrastructure compounds**: cycle-10/11 doc-cycle
   investment (template hygiene generalization +
   filename convention) enabled cycle-1's clean opening
   with zero new infrastructure needed.
2. **Audit cycles have higher signal density than
   mining cycles**: cycle-1 took 2 commits to find +
   ship 2 real gaps from a 5-min `rg` inventory.
   Compare to cycle-2..5 mining-cycle borrow rates
   often < 30%.
3. **Russell's-paradox failure mode** is now a
   documented hygiene-check pattern; future hygiene
   refinements should use multi-line implementation-
   specific signatures unlikely to be quoted verbatim
   in retrospective discussion.

**Standout deliverable**: **a11y-audit-cycle-1** (R255
+ R256) — proves the project can systematically
identify + close a11y compliance gaps using the same
v3.2 methodology + Track F template + filename
convention. 23 invariants ensure no future regression.

**Recommended next cycle direction**: continue audit-
driven cycles (a11y-audit-cycle-2 for keyboard
walkthrough + color contrast) until external mining
opportunities resurface. The project is in **audit-
maturity phase** per cycle-9..11 pattern + cycle-1
inauguration.
